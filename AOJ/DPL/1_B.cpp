#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<n;++i)
const int inf = INT_MAX;

int main(){
    int N,W;
    cin >> N >> W;
    vector<int>v(N),w(N);
    rep(i,N){
        cin >> v[i] >> w[i];
    }
    
    return 0;
}