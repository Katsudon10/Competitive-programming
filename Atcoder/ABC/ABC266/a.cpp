#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<n;++i)
const int inf = INT_MAX;
using ll = long long;

int main(){
    string str;
    cin >> str;
    cout << str.at(str.size()/2) << endl;
    return 0;
}