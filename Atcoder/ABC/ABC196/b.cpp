#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<n;++i)
const int inf = INT_MAX;
using ll = long long;
//fixed << setprecision(5)

int main(){
	char s;
	while (cin >> s) {
		if (s == '.') {
			break;
		}
		else {
			cout << s;
		}
	}
	cout << endl;
    return 0;
}