#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<n;++i)

int main(){
    string str;
    cin >> str;
    sort(str.begin(),str.end());
    cout << str << endl;
    return 0;
}