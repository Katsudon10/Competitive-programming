#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<n;++i)
const int inf = INT_MAX;

int main(){
    int n,k;
    cin >> n >> k;
    vector<string>str(n);
    rep(i,n)cin >> str[i];
    vector<vector<int>>dp(n+1,vector<int>(k+1));
    
    return 0;
}