#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<n;++i)
const int inf = INT_MAX;

int main(){
    int n;
    cin >> n;
    vector<int>num(n);
    rep(i,n) cin >> num[i];
    sort(num.begin(),num.end());
    
    return 0;
}